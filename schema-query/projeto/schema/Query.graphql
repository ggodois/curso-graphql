type Query {
  hello: String!
  currentTime: Date!
  loggedUser: User
  featuredProduct: Product
  lotteryNumbers: [Int!]!
  users: [User]
  user(id: ID): User
  profiles: [Profile]
  profile(id: ID): Profile
}
